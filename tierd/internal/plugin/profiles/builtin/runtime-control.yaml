apiVersion: smoothnas.io/v1
kind: PluginProfile
metadata:
  name: runtime-control
  description: |
    Narrow runtime-socket access for plugins that act as control-plane
    services and create their own short-lived worker containers.
container:
  env:
    DOCKER_HOST: unix:///run/smoothnas-runtime/docker.sock
lxc:
  rawConfig:
    - "lxc.mount.entry = /run/smoothnas-runtime run/smoothnas-runtime none bind,create=dir 0 0"
preflight:
  hostHas:
    - { path: /run/smoothnas-runtime/docker.sock, requirement: required }
