diff --git a/internal/lxc/manager.go b/internal/lxc/manager.go
index 4694ccd..a77bcae 100644
--- a/internal/lxc/manager.go
+++ b/internal/lxc/manager.go
@@ -1020,7 +1020,10 @@ func (m *Manager) createLegacyContainer(id string, imgRec *store.ImageRecord, cf
 		"--newpath", m.lxcPath,
 	).CombinedOutput()
 	if err != nil {
-		return fmt.Errorf("manager: clone %s → %s: %s: %w", imgRec.TemplateName, id, out, err)
+		log.Printf("CreateContainer[legacy]: lxc-copy failed for %s → %s, trying direct copy fallback: %s: %v", imgRec.TemplateName, id, out, err)
+		if fallbackErr := m.copyLegacyContainerDir(imgRec.TemplateName, id); fallbackErr != nil {
+			return fmt.Errorf("manager: clone %s → %s: %s: %w (direct copy fallback failed: %v)", imgRec.TemplateName, id, out, err, fallbackErr)
+		}
 	}
 
 	// Allocate IP for bridge networking.
@@ -1057,6 +1060,74 @@ func (m *Manager) createLegacyContainer(id string, imgRec *store.ImageRecord, cf
 	return nil
 }
 
+func (m *Manager) copyLegacyContainerDir(templateName, id string) error {
+	srcDir := filepath.Join(m.lxcPath, templateName)
+	dstDir := filepath.Join(m.lxcPath, id)
+	srcRootfs := filepath.Join(srcDir, "rootfs")
+	if info, err := os.Stat(srcRootfs); err != nil {
+		return fmt.Errorf("stat template rootfs: %w", err)
+	} else if !info.IsDir() {
+		return fmt.Errorf("template rootfs is not a directory: %s", srcRootfs)
+	}
+
+	if err := os.RemoveAll(dstDir); err != nil {
+		return fmt.Errorf("remove partial clone: %w", err)
+	}
+	if err := os.MkdirAll(dstDir, 0o755); err != nil {
+		return fmt.Errorf("mkdir clone dir: %w", err)
+	}
+
+	out, err := exec.Command("rsync",
+		"-aHAX",
+		"--numeric-ids",
+		"--delete",
+		srcDir+string(os.PathSeparator),
+		dstDir+string(os.PathSeparator),
+	).CombinedOutput()
+	if err != nil {
+		os.RemoveAll(dstDir)
+		return fmt.Errorf("rsync template dir: %s: %w", out, err)
+	}
+	if err := rewriteLegacyCloneIdentity(filepath.Join(dstDir, "config"), filepath.Join(dstDir, "rootfs"), id); err != nil {
+		os.RemoveAll(dstDir)
+		return err
+	}
+	return nil
+}
+
+func rewriteLegacyCloneIdentity(configPath, rootfsPath, id string) error {
+	data, err := os.ReadFile(configPath)
+	if err != nil {
+		return fmt.Errorf("read cloned config: %w", err)
+	}
+
+	lines := strings.Split(string(data), "\n")
+	hostname := sanitizeHostname(id)
+	sawRootfs := false
+	sawUTS := false
+	for i, line := range lines {
+		trimmed := strings.TrimSpace(line)
+		switch {
+		case strings.HasPrefix(trimmed, "lxc.rootfs.path"):
+			lines[i] = "lxc.rootfs.path = dir:" + rootfsPath
+			sawRootfs = true
+		case strings.HasPrefix(trimmed, "lxc.uts.name"):
+			lines[i] = "lxc.uts.name = " + hostname
+			sawUTS = true
+		}
+	}
+	if !sawRootfs {
+		lines = append(lines, "lxc.rootfs.path = dir:"+rootfsPath)
+	}
+	if !sawUTS {
+		lines = append(lines, "lxc.uts.name = "+hostname)
+	}
+	if err := os.WriteFile(configPath, []byte(strings.Join(lines, "\n")), 0o644); err != nil {
+		return fmt.Errorf("write cloned config: %w", err)
+	}
+	return nil
+}
+
 // prepareRootfs ensures runtime directories and resolv.conf exist in the rootfs.
 func (m *Manager) prepareRootfs(rootfs string, cfg ContainerConfig) {
 	// Ensure runtime directories referenced by XDG_RUNTIME_DIR.
