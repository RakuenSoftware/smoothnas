name: CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: read

jobs:
  lint-backend:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: tierd/go.mod

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libpam0g-dev build-essential

      - name: Run go vet
        working-directory: tierd
        run: CGO_ENABLED=1 go vet ./...

  build-backend:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: tierd/go.mod

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libpam0g-dev build-essential

      - name: Build
        working-directory: tierd
        run: CGO_ENABLED=1 go build -o ../bin/tierd ./cmd/tierd/

  test-backend:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: tierd/go.mod

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libpam0g-dev build-essential

      - name: Run Go tests
        working-directory: tierd
        run: CGO_ENABLED=1 go test ./... -count=1

  build-frontend:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: tierd-ui/package-lock.json

      - name: Install dependencies
        working-directory: tierd-ui
        run: |
          echo '//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}' >> .npmrc
          npm ci

      - name: Build
        working-directory: tierd-ui
        run: npm run build

  test-frontend:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: tierd-ui/package-lock.json

      - name: Install dependencies
        working-directory: tierd-ui
        run: |
          echo '//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}' >> .npmrc
          npm ci

      - name: Type check
        working-directory: tierd-ui
        run: npx tsc --noEmit
